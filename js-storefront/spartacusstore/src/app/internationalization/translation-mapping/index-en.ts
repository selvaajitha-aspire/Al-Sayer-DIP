import { address } from '../translations/en/address';
import { asm } from '../translations/en/asm';
import { cart } from '../translations/en/cart';
import { checkout } from '../translations/en/checkout';
import { common } from '../translations/en/common';
import { myAccount } from '../translations/en/my-account';
import { payment } from '../translations/en/payment';
import { product } from '../translations/en/product';
import { pwa } from '../translations/en/pwa';
import { storeFinder } from '../translations/en/store-finder';
import { user } from '../translations/en/user';
import { roadsideAssistance } from '../translations/en/roadsideAssistance';
import { vehicles } from '../translations/en/vehicles';
import { tickets } from '../translations/en/tickets';
import { serviceHistory } from '../translations/en/serviceHistory';

export const en = {
  address,
  asm,
  cart,
  checkout,
  common,
  myAccount,
  payment,
  product,
  pwa,
  storeFinder,
  user,
  roadsideAssistance,
  vehicles,
  tickets,
  serviceHistory
};
